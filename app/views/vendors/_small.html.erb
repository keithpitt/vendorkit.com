<li>
  <strong><%= link_to "#{vendor.name} #{vendor.release.number}", vendor_path(vendor) %></strong>
  <br/><em>Last updated by <%= vendor.release.user.username %> <%= time_ago_in_words(vendor.release.created_at) %> ago</em>
</li>
